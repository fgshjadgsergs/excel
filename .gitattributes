# VBA-файлы: кодировка Windows-1251 + окончания строк CRLF.
# Запрещаем git любую нормализацию текста, чтобы файлы доходили
# до пользователя (в т.ч. через Download ZIP) байт-в-байт.
src/*.bas -text
src/*.cls -text
